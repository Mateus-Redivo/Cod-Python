LISTA DE ESTUDOS - ALGORITMOS E PROGRAMAÇÃO DE COMPUTADORES
3º Bimestre

01. Dado o código:

def somar_um(x):
    return x + 1

def triplicar(x):
    return x * 3

resultado = triplicar(somar_um(4))
print(resultado)

Qual é a saída impressa? Explique o caminho que o valor percorre entre
as duas funções.

02. Uma função tem um parâmetro com valor padrão (default). O que
acontece se ela for chamada passando mais argumentos posicionais do que
ela tem parâmetros definidos?

03. Explique por que, em geral, é considerado boa prática evitar a
palavra-chave global dentro de funções, preferindo receber valores como
parâmetro e devolver o resultado com return.

04. Dado o código:

def calcular_desconto(preco):
    desconto = preco * 0.1
    return desconto

valor = calcular_desconto(200)
print(desconto)

O que acontece ao tentar imprimir a variável desconto na última linha?
Por quê?

05. Dado o código:

def dados_aluno(notas):
    media = sum(notas) / len(notas)
    maior_nota = max(notas)
    return media, maior_nota

m, maior = dados_aluno([7, 9, 5, 10])
print(f"Diferença: {maior - m}")

Qual valor será impresso?

06. Em Python, quando um arquivo tem definições de função misturadas com
comandos de print() fora delas, o corpo de uma função é executado no
momento em que ela é definida, ou apenas quando ela é chamada? Explique.

07. Dado o código:

def matricular(curso, aluno, idade):
    return f"{aluno}, {idade} anos, no curso {curso}"

texto = matricular("Ana", 16, "Python")

Essa chamada gera erro? A ordem dos argumentos importa nesse tipo de
chamada? Explique o que acontece com cada parâmetro.

08. Por que atribuir o retorno de uma chamada a print(...) a uma
variável costuma causar problemas mais adiante no código, quando essa
variável é usada em uma conta ou comparação?

09. Dado o código:

saldo = 100

def depositar(valor):
    saldo = saldo + valor
    return saldo

depositar(50)

O que acontece ao executar essa chamada? Que erro o Python apresenta e
por quê?

10. Dado o código:

def criar_perfil(nome, cidade="Não informado", ativo=True):
    return f"{nome} - {cidade} - {ativo}"

perfil = criar_perfil("Rafael", ativo=False)

O que é atribuído ao parâmetro cidade nessa chamada? Por quê?

11. Qual é a diferença entre uma variável local e uma variável global em
Python? Dê um exemplo simples de cada uma.

12. Por que costuma aparecer um UnboundLocalError quando tentamos alterar
(não apenas ler) uma variável global dentro de uma função sem declarar
global antes?
